************
**DELETE THIS FILE WHEN FINISHED
************

Configuration:

Place folders img and store, and index.php into a NEW folder in your document root. Give the folder an attractive name.

Create the "transparent link" as described in the user manual to the honeypot file, index.php.

Change the $ConfigFile variable to the path of your config file.

Change the $SafeReferer variable to the page with the transparent link to admin.mdb

Delete this README.txt!